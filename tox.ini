[tox]
envlist = py39
[testenv]
deps=flake8       # install pytest in the venvs
commands=flake8  # or 'nosetests' or ...
extend-ignore = E203
exclude =
    .git,
    __pycache__,
    docs/source/conf.py,
    old,
    build,
    dist
max-complexity = 14
max-line-length = 120